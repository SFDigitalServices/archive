{
  "main": "dist/server.js",
  "scripts": {
    "build": "ncc build src/server.ts -o $npm_package_main",
    "watch": "npm run build -- --watch",
    "prestart": "npm run build",
    "start": "nodemon $npm_package_main",
    "develop": "NODE_ENV=development run-p start watch",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.17.3",
    "get-port": "^6.1.2",
    "match-url-wildcard": "0.0.4",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.18",
    "@types/node-fetch": "^2.6.1",
    "@vercel/ncc": "^0.33.3",
    "dotenv": "^16.0.0",
    "jest": "^27.5.1",
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.0.0"
  }
}
